chalk : color output in console (chalk.green('');)

numeral : formatting numbers numeral(1000).format('$0,0.00'); => $1,000.00 



JS tip :
 - use `` to log : `My name is ${name}` which interpretes ${var}
